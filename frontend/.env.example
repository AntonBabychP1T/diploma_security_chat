# For dev (with Vite proxy) nothing is required; override only if deploying without the proxy.
# VITE_API_BASE_URL=https://your-api.example.com
ngrok http 5173 --domain=tender-terrapin-bold.ngrok-free.app
